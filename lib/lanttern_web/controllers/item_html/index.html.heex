<.header>
  Listing Items
  <:actions>
    <.link href={~p"/admin/curricula/items/new"}>
      <.button>New Item</.button>
    </.link>
  </:actions>
</.header>

<.table id="items" rows={@items} row_click={&JS.navigate(~p"/admin/curricula/items/#{&1}")}>
  <:col :let={item} label="Name"><%= item.name %></:col>
  <:action :let={item}>
    <div class="sr-only">
      <.link navigate={~p"/admin/curricula/items/#{item}"}>Show</.link>
    </div>
    <.link navigate={~p"/admin/curricula/items/#{item}/edit"}>Edit</.link>
  </:action>
  <:action :let={item}>
    <.link href={~p"/admin/curricula/items/#{item}"} method="delete" data-confirm="Are you sure?">
      Delete
    </.link>
  </:action>
</.table>
