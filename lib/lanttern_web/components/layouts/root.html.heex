<!DOCTYPE html>
<html lang={LantternWeb.LocalizationHelpers.get_html_lang()} class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" • Lanttern">
      {assigns[:page_title] || gettext("Hey!")}
    </.live_title>

    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#292524" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100..900&family=PT+Mono&family=PT+Sans:ital,wght@0,400;0,700;1,400;1,700&family=PT+Serif:ital,wght@0,400;0,700;1,400;1,700&display=swap"
    />
    <link phx-track-static rel="stylesheet" href={~p"/vendor/glider.min.css"} />
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <%= if ga_id = System.get_env("GA_MEASUREMENT_ID") do %>
      <!-- Google Analytics 4 (Consent Mode v2) -->
      <script nonce={@csp_nonce}>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('consent', 'default', {
          analytics_storage: 'denied',
          ad_storage: 'denied',
          ad_user_data: 'denied',
          ad_personalization: 'denied'
        });
        gtag('js', new Date());
        <%= if scope = assigns[:current_scope] do %>
          gtag('config', '<%= ga_id %>', { user_id: '<%= scope.user_id %>' });
          gtag('set', 'user_properties', { profile_id: '<%= scope.profile_id %>' });
        <% else %>
          gtag('config', '<%= ga_id %>');
        <% end %>
      </script>
      <script async src={"https://www.googletagmanager.com/gtag/js?id=#{ga_id}"}>
      </script>
    <% end %>
  </head>
  <body class="bg-white antialiased min-h-screen font-serif text-base text-ltrn-dark">
    {@inner_content}
    <div
      id="cookies-container"
      class="z-50 fixed bottom-0 inset-x-0 hidden justify-end p-6 sm:p-10"
      phx-hook="Cookies"
    >
      <div class="w-full max-w-xs p-6 rounded-sm bg-white shadow-lg">
        <h6 class="font-display font-bold">
          🍪 {gettext("Lanttern uses cookies...")}
        </h6>
        <p class="mt-4">
          {gettext(
            "We use essential cookies to keep you logged in. We'd also like to use analytics cookies (Google Analytics) to help us improve the app."
          )}
        </p>
        <div class="mt-6 flex gap-4 font-sans">
          <button
            type="button"
            class="font-bold hover:text-ltrn-subtle"
            id="accept-all-cookies-button"
          >
            {gettext("Accept all")}
          </button>
          <button type="button" class="text-ltrn-subtle" id="essential-only-cookies-button">
            {gettext("Essential only")}
          </button>
        </div>
      </div>
    </div>
  </body>
</html>
